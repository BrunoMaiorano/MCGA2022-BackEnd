sum = (a, b) => { return a + b }
rest = (a, b) => { return a + b }
multi = (a, b) => { return a * b }
div = (a, b) => { return a / b }

module.exports = {
    sum: sum,
    rest: rest,
    multi: multi,
    div: div
}
